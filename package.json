{
  "name": "ultistats",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/tests/*.cjs",
    "test:api": "cd api && node --test --test-concurrency=1 tests/*.test.cjs",
    "test:all": "npm test && npm run test:api"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
