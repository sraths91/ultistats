/**
 * @fileoverview Game page module entry point
 * New modular code for the game page goes here.
 * Legacy functionality is still provided by script.js (loaded as regular script).
 */

import { initApp, VERSION } from '../index.js';

console.log(`[UltiStats v${VERSION}] Game page module loaded`);
