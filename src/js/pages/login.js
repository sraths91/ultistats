/**
 * @fileoverview Login page module entry point
 * New modular code for the login page goes here.
 * Legacy functionality is still provided by script.js (loaded as regular script).
 */

import { initApp, VERSION } from '../index.js';

console.log(`[UltiStats v${VERSION}] Login page module loaded`);
